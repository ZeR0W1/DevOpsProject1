---
# Structure only. The real local-environment.yml is generated by
# playbooks/setup_local_environment.yml, uses per-value Ansible Vault encryption,
# and is ignored. Registry values are added on demand only for BUILD_AND_DEPLOY.
admin_email: "admin@example.com"
admin_cidr: "203.0.113.10/32"
db_username: "doa_staging_admin"
dockerhub_username: "example-user"
dockerhub_token: "replace-with-a-Docker-Hub-access-token"