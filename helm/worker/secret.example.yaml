apiVersion: v1
kind: Secret
metadata:
  name: worker-db-secret
  namespace: devops-app
  labels:
    app.kubernetes.io/name: worker
    app.kubernetes.io/part-of: devops-app
type: Opaque
stringData:
  # Dummy example only. Do not put real credentials in Git.
  # The key name must match helm/worker/values.yaml:
  # database.passwordSecretKey: password
  password: dummy-postgres-password
